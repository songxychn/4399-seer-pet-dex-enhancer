# 1.0.3 图鉴回复框修复验证

日期：2026-09-19。页面：https://news.4399.com/seer/tujian/64328.htm 。

根因：旧版图鉴分支在执行 guardReplyDialogs 前提前返回，回复参数校验只在计算器中生效。原站把无关字符串消息解释为评论回复请求，旧版可稳定复现误弹。

验证方式：独立无头浏览器，先注入旧版并发送无关 postMessage 复现，再验证新版关闭无效窗口及后续拦截。新版本覆盖图鉴与计算器，并在 DOM 变化和资源 load 后重试保护，以支持评论脚本延迟加载和入口替换。正常回复只验证窗口打开和关闭，未提交评论。语法构建与 git diff --check 通过。

本轮未使用真实油猴自动注入；测试模拟页面加载后和延迟加载情形，不代表用户具体扩展组合已验证。

1. 旧版图鉴页可复现无关消息误弹回复框。
2. 新版关闭已出现的无效回复框。
3. 图鉴页两个回复入口都已保护。
4. 无关消息不再弹出回复框。
5. 普通评论消息仍能打开回复框。
6. v2 正常评论回复在观察器执行后仍保持打开。
7. 图鉴链接仍只携带正确 ID。
8. 延迟内联脚本重新定义入口后仍受保护。
9. 后加载脚本的 load 事件重新应用保护。
10. 计算器自动选中卡其沙且性格面板正常。
11. 计算器仍拦截无关消息。

主脚本 SHA-256：`844ba3fc75717ee42f48a28144ec8afbadbeebf0dbf55778ee3d69c2d437a3b4`

---

# 1.0.2 精灵 ID 链接回归

日期：2026-09-16。图鉴计算器链接简化为 `#pet=p106`，不再传递 JSON。

本次使用独立无头浏览器访问原站，在 DOM 加载后注入主脚本，读取图鉴实际生成的链接并在新标签加载、注入后验证选择。未访问用户浏览器；未提交评论。Bun 语法构建检查通过。

测试专用 Tampermonkey 配置重启后将原安装入口标为来源不可确定，重新保存未恢复自动注入。因此以下结果属于原站页面逻辑回归，本次未完成 1.0.2 的油猴自动注入及实际点击跳转端到端验证；不沿用 1.0.1 的安装验证作为新版证据。

1. 页面注入 1.0.2 且图鉴链接只携带 ID。
2. 新标签自动选择卡其沙。
3. 普通扩展字符串消息不会误弹回复框。
4. 正常评论回复仍能打开。
5. v2 正常评论入口仍能打开，非法参数被忽略。
6. 四个常用性格在原站页面中计算正确。
7. 全部25种性格仍与原生选择器及修正同步。
8. 错误参数不改变默认选择: pet=p9999999999999999。
9. 错误参数不改变默认选择: pet=bad。
10. 错误参数不改变默认选择: pet=。
11. 错误参数不改变默认选择: pet=constructor。
12. 错误参数不改变默认选择: pet=p106&pet=p4988。
13. 错误参数不改变默认选择: seerPlus=%7B%7D。
14. 无参数保留默认选择。
15. 另一精灵 ID 正确解析。

主脚本 SHA-256：`f911c75fd0c8d3ffa0fa1eeed4994d79d2892562869a139237c53bdb48316934`

---

# 1.0.1 真实油猴回归验证

环境：独立无头 Chrome for Testing 152，Tampermonkey 5.5.0。
测试配置在工作目录内，与用户浏览器配置隔离。已分别验证完整安装脚本及本地 `@require` 入口，后者实测本地文件修改为 1.0.1 后刷新可自动生效。

确认原站评论脚本对任意字符串消息调用回复入口，向未修复的独立测试页发送普通消息可以复现“我要回复”弹窗。尚未定位用户浏览器中具体是哪条消息触发；也未在该测试配置复现用户截图中的“增强逻辑完全没有执行”。

1.0.1 验证直接依靠已安装的 Tampermonkey 自动执行，没有通过页面 eval 注入主脚本。页面 eval 仅用于读状态、发送测试消息及触发控件。正常回复测试只打开、关闭回复窗口，未提交评论。

1. 真实 Tampermonkey 本地依赖加载 1.0.1。
2. 真实自动注入选中卡其沙并创建两个面板。
3. 普通扩展字符串消息不会误弹回复框。
4. 正常评论回复仍能打开。
5. v2 正常评论入口仍能打开，非法参数被忽略。
6. 四个常用性格在真实油猴环境中计算正确。
7. 全部25种性格仍与原生选择器及修正同步。
8. 从图鉴新打开标签自动选中且没有回复遮罩。

迁移并更名之前、经过回归验证的 1.0.1 主脚本 SHA-256：`73f7939b04c9650605ca3ae1aa6ff4bc86dbc3cb807d935ed9264b7be3e09484`

以下保留 1.0.0 的历史验证记录，其“未测试真实油猴”限制已由上面的新增测试补充。

---

# 验证记录

日期：2026-09-16（Asia/Shanghai）
版本：1.0.0
环境：独立无头 Google Chrome for Testing 152；真实 4399 桌面页面。

## 方法与边界

将交付的 `.user.js` 原文注入测试页，调用真实图鉴和计算器的 DOM、数据与计算函数。图鉴到计算器使用实际链接点击验证，新标签页再注入脚本模拟脚本管理器的 document-end 行为。未访问用户登录会话，未向用户现有浏览器安装扩展或脚本。

Tampermonkey 安装及自动注入流程尚未实测；手机站不在本版范围。原站内容与计算公式以本次公开页面为准。

## 已通过检查

1. 图鉴链接携带主精灵、图鉴路径，并在新标签打开。
2. 切换水草蛙后仍明确携带主精灵及浏览形态。
3. 实际新标签选中卡其沙，种族值正确且有形态说明。
4. 两个计算区域均包含25种性格和4个常用按钮。
5. 重复注入后的面板数量仍为2。
6. 选择性格保留已输入的学习力和等级。
7. 常用性格 1 经原生表单计算得到预期六项数值。
8. 常用性格 5 经原生表单计算得到预期六项数值。
9. 常用性格 9 经原生表单计算得到预期六项数值。
10. 常用性格 10 经原生表单计算得到预期六项数值。
11. 全部25种性格均同步原生选择器与五项修正。
12. 自定义修正清除性格高亮与过期结果，点击性格可恢复。
13. 原生性格下拉框与增强面板双向同步。
14. 个体值计算隐藏体力性格；攻击按固执正确反推31个体。
15. 匹配失败保留原站选择并显示提示。
16. 不可信名称只显示文本、不注入HTML。
17. 损坏的链接参数安全降级，性格面板仍正常。
18. 直接进入计算器不强制选择历史精灵。
19. 测试前默认记录为卡鲁维德。
20. 新图鉴路径识别卡鲁维德。
21. 新图鉴链接独立选择卡鲁维德。
22. 另一个标签独立选择卡其沙。
23. 返回前一标签仍保留卡鲁维德，多个标签不串台。

语法检查：Bun 浏览器构建解析通过；交付文件保持原始可读代码，未压缩。

可访问性检查：新增能力值性格面板在折叠状态下，axe-core 4.12.1 检出 0 项违规（13 项通过，0 项待确认）。此结果仅针对新增面板，不代表原站整体无障碍审计通过。

## 计算核对样本

卡其沙，100 级，31 个体，各项学习力为 0。顺序为体力、攻击、防御、特攻、特防、速度。

| 性格 | 原站实际输出 / 预期输出 |
| --- | --- |
| 固执 | 339、215、212、212、224、210 |
| 保守 | 339、176、212、259、224、210 |
| 胆小 | 339、176、212、236、224、231 |
| 开朗 | 339、196、212、212、224、231 |

## 文件校验

`seer-4399-enhancer.user.js` SHA-256：

```
51a888a108bc32c5a41037db894a182e8ad6cc532b576709f216d1652a37a5a6
```

## 仓库迁移

2026-09-16：主脚本迁入 Git 仓库，仅更新中英文名称元数据，运行逻辑未改动。上方校验值对应更名前的交付文件。
